AI 파트 코드 전부 merge했습니다.
저희 둘 다 저게 완성본이 아니고 test일거라 가볍게 돌아가게만 정리해뒀어요!
만약 바뀌는 부분 있더라도 각 기능별 폴더에서 구현하면 돼서 충돌은 없을거같긴해요  
이거 가져가서 오류나면 꼭 말해주세용..

====
2026.03.11 12:12 기준 변경사항

1. AI test1,test2 merge 
2. AI_test1 의 gitignore 정리 (AI 폴더에 중복되어 있던 파일 삭제 및 내용 정리)
3. AI_test1 의 폴더명 정리 (ai, test 삭제)
4. src 경로 설정

예시)
현재 파일과 src 폴더가 같은 위치에 있을 때, from src.~ 가 인식 안되는 경우가 많았음
```
# src 경로 설정
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
```

5. db 연결 폴더 이름 변경 (src->source) 다른 src와 용도 구분
6. requirements 추가

===
요청사항

1. 사용한 라이브러리들 requirements/ai.txt에 잘 정리해주세요! 테스트할 때 없어서 오류난 경우가 많았어요. 일단 제가 테스트하면서 없었던 라이브러리&패키지들은 새로 추가해놨는데 한 번 보고 추가로 확인부탁드려요

2. 팀원 공유 목적으로라도 주석 부탁드려요. 주석이 없다보니 코드 자체를 읽느라 의미 파악하기가 힘들었어요 ㅠㅠ 아마도 나중에 작업할 때도 예전 코드 볼 때 도움 될거에요!

3. 아마 파일 정리를 하시면서 꼬인 것 같은데 db에서 불러올 때 text라는 없는 컬럼을 불러온다던지 하는 코드들이 있더라구요(text->body로 변경함) push하실 때 한 번만 체크해주시면 감사하겠습니당

